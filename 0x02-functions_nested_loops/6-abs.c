#include "main.h"

/**
 * _abs - function that computes the absolute value of
 * an integer
 *
 * @v: function's arguement
 *
 * Return true (n)
 */

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
