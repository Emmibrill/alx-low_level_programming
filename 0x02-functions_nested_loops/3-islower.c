#include "main.h"

/**
 * _islower - function that checks for lowercase character
 *
 * alp: alphabet
 * Return true
 */

int _islower(int c)
{
if (alp <= 'a' && alp <= 'z')
{
return (1);
}
else
{
return (0);
}
}
